/**
 * @handle {
 *  "icon": "lucide:folder-open-dot",
 *  "order": 2
 * }
 */

import { replace } from 'react-router-dom';
const Document = () => {
  return null;
};

export const loader = () => {
  replace('router');
};

export default Document;

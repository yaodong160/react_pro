/**
 * @handle {
 *   "hideInMenu": true,
 *   "activeMenu": "/annotation",
 *   "title": "摄像头采集"
 * }
 */

import { Outlet } from 'react-router-dom';

const CameraLayout = () => {
  return <Outlet />;
};

export default CameraLayout;

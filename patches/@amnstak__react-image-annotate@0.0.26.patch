diff --git a/dist/ImageViewer.js b/dist/ImageViewer.js
index 4fbfd712b59c18f3280a17a41e4892dfa726bff7..0000000000000000000000000000000000000000 100644
--- a/dist/ImageViewer.js
+++ b/dist/ImageViewer.js
@@ -2,7 +2,12 @@ import { useRef as S, useLayoutEffect as ee, useState as ae, useEffect as ne, cr
 import { jsx as W, jsxs as $ } from "react/jsx-runtime";
 import { m as q, g as fe, t as xe, b as Y, c as de, d as pe, P as te, R as ge, I as ye, f as ve, h as be, u as we } from "./index-BLVsk0vl.js";
 import { x as ie, c as me, T as Me } from "./Portal-IhUi94Sd.js";
-const Te = () => q.Matrix.from(1, 0, 0, 1, -10, -10), Ee = ({
+const Te = (layoutParams) => {
+  if (layoutParams && layoutParams.iw && layoutParams.canvasWidth) {
+    return q.Matrix.from(1, 0, 0, 1, -(layoutParams.canvasWidth - layoutParams.iw) / 2, -(layoutParams.canvasHeight - layoutParams.ih) / 2);
+  }
+  return q.Matrix.from(1, 0, 0, 1, -10, -10);
+}, Ee = ({
   canvasEl: u,
   changeMat: l,
   changeDragging: g,
@@ -23,9 +28,18 @@ const Te = () => q.Matrix.from(1, 0, 0, 1, -10, -10), Ee = ({
 }) => {
   const c = S({ x: 0, y: 0 }), I = S({ x: 0, y: 0 }), j = (e, t) => {
-    const [o, x] = [t.x, t.y];
-    let h = typeof e == "object" ? e.to / r.a : 1 + 0.2 * e;
-    r.translate(o, x).scaleU(h), r.a > 2 && r.scaleU(2 / r.a), r.a < 0.05 && r.scaleU(0.05 / r.a), r.translate(-o, -x), l(r.clone());
+    const [mx, my] = [t.x, t.y];
+    const oldScale = r.a;
+    let targetScale = typeof e == "object" ? e.to / oldScale : 1 + 0.1 * e;
+    const newScale = oldScale * targetScale;
+    if (newScale > 3) targetScale = 3 / oldScale;
+    if (newScale < 0.05) targetScale = 0.05 / oldScale;
+    // Keep the point under the mouse fixed during zoom
+    r.a *= targetScale;
+    r.d *= targetScale;
+    r.e = r.e + mx * oldScale * (1 - targetScale);
+    r.f = r.f + my * oldScale * (1 - targetScale);
+    l(r.clone());
   };
   return { mouseEvents: {
     onMouseMove: (e) => {
@@ -72,7 +86,7 @@ const Te = () => q.Matrix.from(1, 0, 0, 1, -10, -10), Ee = ({
           );
           M < 0.05 && (M = 0.05), M > 10 && (M = 10);
-          const B = Te().translate(s.x, s.y).scaleU(M);
+          const B = Te(n.current).translate(s.x, s.y).scaleU(M);
           l(B.clone());
         }
         v(null), a(null);
diff --git a/dist/lib.js b/dist/lib.js
index acd3e2accddab5519c664052b047c3b5e9362e46..0000000000000000000000000000000000000000 100644
--- a/dist/lib.js
+++ b/dist/lib.js
@@ -4105,7 +4105,12 @@ const Mu = /* @__PURE__ */ ga(gH), yH = $r(), mH = Ls.create({
         left: 0
       }
     )
-] })), kH = () => r1.Matrix.from(1, 0, 0, 1, -10, -10), SH = ({
+] })), kH = (layoutParams) => {
+  if (layoutParams && layoutParams.iw && layoutParams.canvasWidth) {
+    return r1.Matrix.from(1, 0, 0, 1, -(layoutParams.canvasWidth - layoutParams.iw) / 2, -(layoutParams.canvasHeight - layoutParams.ih) / 2);
+  }
+  return r1.Matrix.from(1, 0, 0, 1, -10, -10);
+}, SH = ({
   canvasEl: e,
   changeMat: t,
   changeDragging: n,
@@ -4126,9 +4131,19 @@ const Mu = /* @__PURE__ */ ga(gH), yH = $r(), mH = Ls.create({
 }) => {
   const k = li({ x: 0, y: 0 }), S = li({ x: 0, y: 0 }), O = (A, T) => {
-    const [L, K] = [T.x, T.y];
-    let N = typeof A == "object" ? A.to / d.a : 1 + 0.2 * A;
-    d.translate(L, K).scaleU(N), d.a > 2 && d.scaleU(2 / d.a), d.a < 0.05 && d.scaleU(0.05 / d.a), d.translate(-L, -K), t(d.clone());
+    const [mx, my] = [T.x, T.y];
+    const oldScale = d.a;
+    let targetScale = typeof A == "object" ? A.to / oldScale : 1 + 0.1 * A;
+    const newScale = oldScale * targetScale;
+    // Clamp scale range
+    if (newScale > 3) targetScale = 3 / oldScale;
+    if (newScale < 0.05) targetScale = 0.05 / oldScale;
+    // Keep the point under the mouse fixed during zoom:
+    // new_e = old_e + mx * oldScale * (1 - targetScale)
+    d.a *= targetScale;
+    d.d *= targetScale;
+    d.e = d.e + mx * oldScale * (1 - targetScale);
+    d.f = d.f + my * oldScale * (1 - targetScale);
+    t(d.clone());
   };
   return { mouseEvents: {
     onMouseMove: (A) => {
@@ -4179,7 +4194,7 @@ const Mu = /* @__PURE__ */ ga(gH), yH = $r(), mH = Ls.create({
           );
           W < 0.05 && (W = 0.05), W > 10 && (W = 10);
-          const ne = kH().translate(r.x, r.y).scaleU(W);
+          const ne = kH(l.current).translate(r.x, r.y).scaleU(W);
           t(ne.clone());
         }
         i(null), a(null);
@@ -4559,7 +4574,10 @@ const Mu = /* @__PURE__ */ ga(gH), yH = $r(), mH = Ls.create({
   }
 }), $v = (e = null, t = null) => {
   let n = r1.Matrix.from(1, 0, 0, 1, -10, -10);
+  if (t != null && t.iw && t.canvasWidth) {
+    n = r1.Matrix.from(1, 0, 0, 1, -(t.canvasWidth - t.iw) / 2, -(t.canvasHeight - t.ih) / 2);
+  }
   return e && (t != null && t.iw) && (n = n.translate(e.x * t.iw, e.y * t.ih).scaleU(e.w + 0.05)), n;
 }, $H = ({
   regions: e,
